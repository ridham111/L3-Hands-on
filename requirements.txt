# --- Core agent ---
pydantic>=2.6,<3.0
python-dotenv>=1.0

# --- The agent runtime (the ONLY LLM backend) ---
claude-agent-sdk>=0.2  # Anthropic agent harness: owns the tool-use loop. Bundles the
                       # Claude Code CLI; runs on a Claude Pro/Max subscription via OAuth.
httpx>=0.27          # HTTP transport (used by clone/ingest + SDK deps)
truststore>=0.9      # verify TLS via OS trust store (corporate proxies)

# --- RAG / vector index ---
scikit-learn>=1.4        # TF-IDF store (offline default, no model download)
numpy>=1.26
fastembed>=0.3           # dense semantic embeddings (local ONNX, no torch, no key)
                         # set ONBOARDING_VECTOR_BACKEND=dense to use it (recommended)

# --- Access layer ---
fastapi>=0.110
uvicorn[standard]>=0.29
typer>=0.12

# --- Tests ---
pytest>=8.0
